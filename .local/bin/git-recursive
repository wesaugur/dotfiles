#!/bin/sh -eu

GIT_ARGS=${*:-status}
DIR=${DIR:-.}

find "${DIR}" -name '.git' -exec sh -c \
        'REPO=$(dirname ${1}) && echo ${REPO} && cd ${REPO} && git ${2} && echo' \
        _ {} "${GIT_ARGS}" \;
