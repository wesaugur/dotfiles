#!/bin/sh -eu
#!/bin/dash -eux    # For POSIX-compliance during development

GIT_ARGS=${@:-status}
DIR=${DIR:-.}

find "${DIR}" -name '.git' -exec sh -c 'REPO=$(dirname {}) && echo ${REPO} && cd ${REPO} && git ${0} && echo' "${GIT_ARGS}" \;
